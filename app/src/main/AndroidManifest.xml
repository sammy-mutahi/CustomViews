<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sammy.customlayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomLayouts">
        <activity
            android:name=".paths.PathActivity"
            android:exported="false"
            android:label="@string/title_activity_path"
            android:theme="@style/Theme.CustomLayouts" />
        <activity
            android:name=".paths.genderPicker.GenderPicker"
            android:exported="true"
            android:label="Gender Picker"
            android:theme="@style/Theme.CustomLayouts">
        </activity>
        <activity
            android:name=".tictac.TicTacToeGame"
            android:exported="true"
            android:label="Tic Tac Toe"
            android:theme="@style/Theme.CustomLayouts"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CustomLayouts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>